# Alerting Review Principles

审核与构建「服务故障告警」质量的行为准则——覆盖**值不值得 page、多严重、说什么、要不要合并**。

**目标**：让对的人、在对的时间、采取对的行动，且不"狼来了"。设计上要同时对抗两个失败模式——**漏报**真实事故（silence 看着和健康一样），和**告警疲劳**被用户静音（被静音的告警 = 没有告警）。

**取舍**：本准则偏向**更少、更高信噪比**的告警，而非为覆盖而多报。信息性/诊断性内容降级到低通道或落状态不推送，而不是都用 page 级轰炸。

**这套原则奏效的标志**：运维读消息本身即知影响与该做什么；page 级（🔴）只在需要立即处置时响；一个根因只发一条通知；没有一条告警会被用户调成静音。

**Loop**：对每条告警规则和每条告警消息，逐条对照本档**全部**原则，循环到无原则被违反为止（纯审查场景无权改这些 artifact，把无法消除的违反项交给它的 owner，不要静默停下）。本档的原则分两类：**fire-质量**类裁决"这条告警该怎么发"，按下文顺序裁决彼此冲突；**可行性 / 前提**类裁决"这条告警在目标部署能不能成立"，不进那个序——但同样每条都要过，不在排序里不等于不在 Loop 里。**归属由每条原则在自己末尾显式声明**，没声明的即 fire-质量类；别按编号位置推断，也别让读者自判。

**配套档**：消息内容的通道无关判据住在 `human-facing-message-principles.md`（面向人的状态/结果消息通用），本档 P3/P4 只承载它在 push 通道的投影——审查 P3/P4 时两档一起读，并**承接该档的裁决顺序**（内容必须在主视图 > 主视图裁剪），不用本档 P3 早于 P4 的编号去重排它。

---

## 与 `service-operations-protocol.md` §6 的边界

两档各管一层，交叉引用、不重复。**构建和审核告警都要过这两档。**

| 关注点 | Home |
|---|---|
| 这个服务要不要 push 告警 | §6.1 |
| 怎么投递：`im-notify --alert --dedup-key`，别手搓 webhook | §6.3 |
| 同一「问题身份」的重复用 dedup-key 去重、状态变化才发 | §6.4 |
| **哪些条件值得 page**（症状 vs 内部阈值；抑制已知良性） | **本档 P1** |
| **严重度校准** | **本档 P2** |
| 消息内容的**通道无关**判据（影响 / 行动 / 紧迫度 / 结论作用域 / 可读性） | `human-facing-message-principles.md` |
| **消息内容在 push 通道的投影**（严重度前缀与正文一致、一屏预算） | **本档 P3、P4** |
| **不同 key、同根因的多告警合并** | **本档 P5** |
| 证据链接、确认与闭环 | **本档 P6、P7** |
| **告警流留痕**（持久 / 有界 / 可查） | **本档 P8** |

§6.4 的 dedup-key 处理**同一 key** 的重复刷屏；本档 P5 处理**不同 key、同一根因**的扇出——两者不可互相替代。

---

## 优先级与冲突裁决

**fire-质量**类的原则按**裁决优先级**排列，两条给出冲突指导时编号小者胜：值不值得 page > 严重度 > 说什么 > 怎么说 > 合不合并 > 证据 > 生命周期 > 留痕。新增一条时按它裁决的是哪件事插进这个序，不是追加到末尾。**可行性 / 前提**类不进这个序（见 Loop 行的分类）。

**逃生阀**：当照此顺序会与你对"什么服务于告警目标"的判断相悖时，按 `human-facing-message-principles.md`「裁决顺序」的逃生阀走（构建中问用户；纯审查场景把冲突交消息 owner）。

---

## 1. 症状优先，抑制已知良性

**告警条件应追踪用户可感知的影响（或迫近的影响），而非纯内部指标破线。代理/provisional 信号（同机 co-load、合成探针、已知瞬态生态）只有在有独立信号背书——该信号覆盖它所探测的同一用户可感知失败面（且对该面的真实退化至少同等敏感、不因阈值更钝而漏）、且当前干净——时才可降级或抑制；否则（唯一探测路径，或只有位置更贴近用户却不覆盖同一失败面的信号）保留 page 资格。"是 provisional"只 correlate、不 entail 良性；"位置更贴近用户"也不 entail 覆盖同一失败面。**

flag：
- 条件是内部阈值破线，但说不清哪个用户的什么体验会坏。
- 对代理/provisional 信号降级或抑制，却**没有**覆盖同一失败面的独立干净信号背书——此时它可能是唯一探测路径，降级会静默掉真实事故。
- 反过来：已有覆盖同一失败面的独立干净信号佐证良性，却仍对代理信号以最高级 page（这才是真的过度 page）。

自检：这条一响，能不能一句话说出哪个用户的体验坏了？说不出别急着判它良性——回到上面的规则走一遍。

---

## 2. 严重度 = 影响 × 紧迫

**严重度按影响与紧迫定，不按信号是否 provisional 定：page 级/最高级只保留给真实或迫近的用户伤害、且需要立即处置的情形；良性 / 信息性走低档通道或只落状态、不推送。**

flag：
- 同一严重度既给"服务挂/上游不可用"，又给"provisional 探针偏慢"这类不对等的事。
- 不需要立即行动的信息也用 page 级推送打断人。
- 完全无分级——所有告警一个通道、一个颜色。

自检："收到这条，我必须现在放下手里的事吗？"否 → 它不该是 page 级。

---

## 3. 消息自解释：影响 + 行动 + 是否需立即处置

通道无关的判据见 `human-facing-message-principles.md` §1（影响 + 行动 + 紧迫度）与 §2（结论作用域不超过证据作用域），其「先定读者情境」对 push 告警的归档：上下文**不在手**（通道不保证同步读取，须完全自解释）、正文段为 **triage**（附带的诊断 dump 段另按诊断标尺）。体长硬限放不下作用域限定时，按该档「裁决顺序」不得牺牲 §2——结论是不发这条推送（回到 P1 的"值不值得 page"），而非发一条无限定的"OK"。判据是**读者会不会得出一个结论**，不是这条推送有没有写下一个：一条只说探测器自己挂了、对被监控服务只字未提的告警不含任何显式结论，读者照样据它判断服务状态。**审查本条必须连同该档一起读**。以下是它在 push 通道的投影。

**读者是被消息打断的人，不是主动来看的人：紧迫度必须落在严重度前缀与通道选择上（🔴 page = 现在处置 / 通知 = 仅知会），行动必须写进正文，不能只存在于链接后面。**前缀与通道只有这两档；真实处置窗口比它细时（"下个维护窗口必须处理"），第三档由正文的"需否立即处置"字段承载——共享档 §4 的分辨力要求在此按正文判，不要求细分前缀。

flag（通用 flag 之外）：
- 严重度前缀与正文实际要求的处置速度**方向**不符（🔴 但正文说"观察即可"；仅知会但正文要求现在放下手里的事）。正文在前缀档位内进一步细化处置窗口不算不符——那正是上面第三档的承载方式。
- 正文没有可执行的第一步，行动只在 runbook 链接里。
- 告警声称覆盖某失败面，但该面本轮实际未被测到（基线饥饿、探针失败），消息未声明——与 P9 同源，此处是消息层的表现。

---

## 4. 为压力下的人写

通道无关的判据见 `human-facing-message-principles.md` §3（主视图按决策相关性组织）与 §4（值的呈现服从决策用途）——**审查本条必须连同该档一起读**。以下是它在 push 通道的投影。

**读者可能是被吵醒的人，只有一屏、没有上下文：推送正文是主视图，调试细节进 evidence / runbook，不进正文。**

flag（通用 flag 之外）：
- 一条推送里塞进多屏调试信息（push 没有"滚回去看"，正文长度就是全部预算）。
- 需要另开一个系统才能理解正文在说什么。

自检：正文里每个片段，运维**决策**时用得上吗？用不上 → 移到 evidence/runbook。

---

## 5. 一次事故 = 一条通知

**同一根因引发的多条关联告警必须合并成一条带清单的通知。**

flag：
- 一轮检测里多条互不相同的告警几乎同时响，且指向同一根因（如主机争用让所有旅程一起超标）。
- 无关联/rollup 逻辑，每个规则各自独立推送。
- 运维要自己在脑子里把 N 条拼回 1 个事故。

自检：这些同时响的告警共享同一可观察的**因果**判据吗（同一上游标识 / dedup 维度；"同主机"仅在假设根因本身即主机级——如 CPU 争用——时才算，否则只是位置巧合、不 entail 同因）？是 → 合并成一条带明细清单的通知；无可核验的共因判据、仅时间巧合时倾向不合并，别把独立并发的第二事故并掉。

---

## 6. 每条告警指向证据/runbook

**每条告警带"去哪看"——evidence artifact、日志路径或 runbook 章节——让 triage 不从零开始。**

flag：
- 告警没有任何"下一步去哪看"的指针。
- 处置动作抽象、无具体落点（哪个文件、哪个页面、哪条命令）。

自检：收到告警的人能否不用翻找、直接知道该打开哪个文件/页面/命令？

---

## 7. fire 与 resolve 各是一次断言，各自要有证据

**抖动/易 flap 的信号先去抖（确认窗口 / 连续失败计数）再 fire，避免单点毛刺 page；只有 supervisor 已放弃重试的硬 down 才 entail 终态、应立即 page，强行去抖只会平添延迟。恢复后发显式 resolve，让运维知道事故结束——但 resolve 与 fire 同理，也要拿得出证据。投递侧的去重与复位见 §6.4，本条是设计侧的"确认"与"闭环"。**

flag：
- 对**抖动-prone**信号、或对目标状态的**远端单次采样**（端口 / 健康端点）单次失败即 page、跳过去抖。
- 把**裸 PID 消失 / 单次 exit 事件**当终态即时 page——计划重启 / rolling deploy / crashloop 每周期都会瞬时 PID 消失或发 exit，据此即时 page 会把本条要防的 flapping-page 放回来。
- 反过来：对 supervisor 已放弃重试的硬 down 强行要求确认窗口，给本该即时的 page 加延迟。**该判据要落到目标部署真有的那个量上**：systemd 的重启预算、crashloop 阈值是常见取值，但无 supervisor 的部署（cron / one-shot / 手起的常驻脚本）里它恒不成立，`Restart=always` 且未设 burst 的容器里它永不到来——此时"已放弃重试"要另找证据，否则一个死透的服务永远不满足终态判据。
- 去抖的免除只按"能否自愈"收窄，而不看**该失败面的损失速率**：数据丢失、支付通道报错、误删既 flap-prone 又每分钟有实打实损失，也不是硬 down，于是被无条件加上确认窗口。
- fire 后无 resolve 通道，运维不知道好没好。
- **fire 判据是"当前异常项列表非空"，而那个列表的输入集会缩小**（对象掉出评估集、分区被删、采集端下线、租户下架），却把列表变空无条件当作恢复。此时"异常解除"与"我不再看得见它"产生**同一个**读数，于是告警会在对象最彻底失效的那一刻宣布恢复。
- 反向的漏洞：把"我看不见了"一律判成不许 resolve。对象是**被有意撤走的**（租户主动下架、分区按计划删除、vantage 停用）时，这条告警将永久 firing 且永远拿不出"条件解除"的证据——正确动作是退役它，不是让它挂着变成静音候选。

自检：**这条告警的每一次状态转换，拿得出什么证据？** fire 侧——触发它的读数会不会由一次自愈得了的扰动产生（计划重启、leader 切换、配置 reload、上游 LB 抽出重入都算）？resolve 侧——判据的输入集会不会缩小？会的话，先分清是"条件解除"、"我看不见了"，还是"这个对象已经不该被监控了"，三者的读数相同而处置各异。

---

## 8. 告警留痕：持久、有界、可查

**fired 告警应留下持久、可查询的记录，且该记录必须有界——留痕缺失则事故后无据可查（无法复盘、调阈值），有界确保告警流不成为 retention 的遗漏项。retention/rotation 的通用机制见 `resource-cleanup-protocol.md`，本条只约束"告警流要留痕、且留痕要有界"。**

flag：
- fired 告警只落当前状态（覆盖写）或仅进程 stdout，无持久可查的历史——事故后无法回答"这条规则近来告警过几次、每次什么值"。
- 有留痕但无 retention/rotation，随时间无界增长。

自检：事故后能不能查到"什么告警、何时、多频、什么值"？这份记录会不会无限涨？

## 9. 判定路径不得在真故障时静默失效

**一条告警的判定路径由若干输入喂养——基线、样本量、确认窗计数、阈值取值、评估触发条件、作用域。每一个都要问同一件事：故障真的发生时，它朝哪边走？答案是"朝更像健康的方向"或"干脆不再执行"，这条告警就在最需要它的时刻沉默。这类失效比误报隐蔽得多——监控看着在，其实是瞎的，而沉默与健康在所有观察面上同形。**

flag：
- fire/降级/rollup 依赖一个基线或最小样本量，但没人验证部署里真产出得了它（量 + 新鲜度）。
- 前提**本应产出却被饥饿**（如 idle 采样被并发 pipeline 常态挤空），告警却静默地永不 fire、对外伪装"一切正常"，而非显式报"无法评估 / 监控降级"。**饥饿是结构性的**（常态挤空而非偶发）时，那条降级通知会每轮都发、退化成常驻噪声——此时要修的是采样保障或告警本身的生命周期，不是每轮再喊一次。
- 判定量从被监控对象自身派生，故障方向恰好使它放宽：`threshold = f(近期样本量 / 近期速率)`（`2 × 窗口时长 / 窗口内条数`、`k × 近期均值 QPS` 都是这个形状）。**两个方向都算**——量降型故障里对象停摆压低基线、阈值变松；量升型故障（重试风暴、失控循环、成本失控、队列积压）里 `k × 近期均值` 随风暴一起抬高，把风暴正常化。
- 判定量的取值可被与真实健康水位无关的输入支配：一条数年前的回填记录、一次迁移导入，就能把"典型间隔"抬到任何真实静默都够不着的位置。
- 判定路径上有一环在故障态**不再执行**：评估由对象自身产出事件触发（对象停摆即不再评估）、去抖要求"连续 N 次采样失败"而死后采不到样本、比值型指标的分母归零、严重度按近期流量分档而流量归零即降为 info。
- 作用域限定（我覆盖了多少、漏了多少）**只写在健康态那条消息里，firing 时被丢弃**。降级在两条路径上都成立，而运维恰好是读 firing 那条时才动手——把它只放在"没事"的分支，等于在唯一需要它的时刻缺席。同一形态也出现在结构化 payload 里带了、渲染层从不输出它。

前提确实没产出时，区分**被饥饿**与**本就不该有**：前提本应在该部署产出、却被挤空/太慢/太旧 → 该告警瞎了，退化成 fail-closed 默认（显式报"无法评估/监控降级"）。前提在该部署**本就合法缺席**（可选集成被关、该 vantage 未启用）→ 干净跳过、根本不 arm 这条告警，别把一个故意关掉的功能变成常驻降级通知（那是自造告警疲劳）。二者自检都返回"基线没产出"，但结论相反。**注意"合法缺席"要由目标侧的显式声明认定**（配置里写明该部署不启用此 vantage），不能只读运行时开关——配置漂移、掉了个环境变量、凭据过期导致集成自我禁用，读数与"我们故意关掉了"一模一样，据它跳过等于在监控被误关的那一刻静默不 arm。

自检：**把被监控对象设为它自己的故障态**（量降型即停摆，量升型即风暴），**从头跑一遍这条告警的判定路径——有没有哪一环朝"更像健康"的方向移动，或干脆不再执行？** 这要真代入算一遍：自适应判据读起来都像在"适应"，方向只有代入才看得出来。另问：依赖的基线/前提在目标部署以**所需量 + 新鲜度**真被产出吗（真实数据接地见 `review-alerting` §0.5——这一问的观察面是生产数据，不是规则和消息的文本，读文本永远问不到它）？作用域限定在 firing 与非 firing 两条路径上都读得到吗？

（本条是**可行性 / 前提**维度，与 fire-质量维度的原则正交，不参与它们的编号冲突裁决；一个告警可同时被本条与它们评判。该豁免只针对 fire-质量维度的编号排序，**不豁免于 `human-facing-message-principles.md`**——本条判「要不要 arm 这条告警」，一旦决定发，该档 §2 照常适用。）

---

## 反例改写（AI Radar，2026-07-20）

**改写前**（实际推送，违反 P1/P2/P3/P4/P5）：

```
【AI Radar】🔴 PERF:wechat.pagination.settle:same_host_origin:busy 旅程性能退化 wechat.pagination.settle
故障类别：旅程性能退化 wechat.pagination.settle
具体故障对象/数值：wechat.pagination.settle same_host_origin busy: samples=35,
  p75=2367.249083ms/1000ms, p95=3521.997208ms/1500ms, advanced_window_streak=3/3;
  same-host provisional; not a regional SLO
处置方向：查看证据中的近期样本、CPU 与 pipeline 状态；该结果仅代表同机 provisional 观测。
```

违反 P1–P5：busy 良性仍 🔴 且无独立干净信号背书（P1）/ 与 A1 同级（P2）/ 埋掉"用户大概率无感"的结论（P3）/ 假精度 + streak/samples 内部量入正文（P4）/ 6 条同因（host busy）齐发未合并（P5）。

**改写后**（降级 + 合并，符合 P1–P5；下方字段布局仅为示范、非规定格式）：

```
【AI Radar】🟡 性能自测（同机 provisional，非用户侧）：pipeline 运行期间 6 条旅程探针超预算
影响：仅同机合成探针，与 pipeline 并发、疑似主机 CPU 争用；idle 视角与真实用户侧无异常，用户大概率无感。
需否立即处置：否——除非 idle 视角也开始超标（那才代表真实退化）。
去哪看：logs/performance/evidence/ 最新证据 + 主机 load。
明细：homepage.first_card、wechat.list、wechat.pagination 等 6 条在 busy 下超预算
  （最严重 homepage p95 ~20s，含探针超时）。
```

修复 P1–P5：🔴→🟡 降级（P1/P2，因 idle 与真实用户侧这两个独立信号在场且干净）/ 开篇即写影响与"需否处置"（P3）/ 去假精度与内部量、数值带对照（P4）/ 6 条合一（P5）。
