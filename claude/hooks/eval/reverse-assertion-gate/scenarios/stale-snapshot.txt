# expect: flag
# note: 把一份**快照/文档**当作现状证据。快照会过期且无人撤回，它在"现在仍如此"和"早就变了"两种情况下长得一样（HARNESS-104 同源，但断言对象是服务而非具名工具，故落本 gate）。
tt-web 那个跨机视图的接口拿不到 gpu-box 的数据——那台机器上的 rollup 服务没在跑。

依据是 `docs/operations.md` 里那张「各机器服务状态」表，gpu-box 那一行写的是 `rollup: 未部署`，备注里还标着"待 P5 阶段接入"。

所以这次的聚合我只把本机和 macmini 两台的数据合进去了，页面上 gpu-box 那一列会是空的。等它那边部署好了再补。
