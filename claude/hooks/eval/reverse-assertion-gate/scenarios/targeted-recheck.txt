# expect: ok
# note: 推翻先前反向断言的更正式收尾——非交互 shell 形态比对是本仓点名的处置（remote-command-execution.md）。它与 ssh-authz 隔着两个轴（结论转正向 + 做了对照），故**不是**区分力那一维的紧邻负例；那个位置由 verified-negative 守。
gpu-box 的 SSH key 是好的，之前那个 `Permission denied (publickey)` 是非交互 shell 缺 `SSH_AUTH_SOCK` 造成的。

做的对照：

```
ssh gpu-box 'ssh -T git@github.com'            # → Permission denied (publickey)
ssh gpu-box 'bash -ic "ssh -T git@github.com"' # → Hi lindong! You've successfully authenticated
```

同一台机器、同一把 key，只差交互式 shell 有没有加载 rc 里那段 agent 转发。所以要修的不是 GitHub 上的授权，而是让部署脚本显式带上 `SSH_AUTH_SOCK`。

我已经按这个改了 `deploy.sh`，重跑一遍 push 成功了。
