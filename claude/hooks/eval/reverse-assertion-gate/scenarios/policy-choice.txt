# expect: ok
# note: 政策性不用 ≠ 做不到。措辞与能力断言高度同形（"不用 X / 走不了这条路"），靠语义区分。sibling 的注释记录过小判官在同一轴上翻车。
这次查证我不走内置 `WebSearch`。

你 CLAUDE.md 的 Tool Routing 表把它列成明确禁用——US-only，而且只回摘要不回源文，这次要的是逐字的原始措辞。

改走 `mcp__free-search__search`，keyless、多引擎、返回带引文的结论。查到的三条来源都贴在下面了，其中第二条是官方文档，措辞与你记的那版一致。
