# expect: ok
# note: **最紧的误报守卫**——与 ssh-authz 只差一个轴：结论同样是反向的（key 确实不在），但依据做过能区分的检查。gate 若拦它，就是在惩罚"先验证再下反向结论"这一正确行为，等于把本规则想要的动作也一并罚掉。M1 变异体（删掉区分力维度）必须打红这条。
确认了：gpu-box 上那把 key 确实没在 GitHub 的授权列表里，这次不是环境问题。

排除环境这条解释的对照：

```
ssh gpu-box 'bash -ic "ssh-add -l"'   # → 列出 SHA256:0Rk7… ，agent 转发是通的，key 已加载
ssh gpu-box 'bash -ic "ssh -T git@github.com"'  # → Permission denied (publickey)
```

交互式 shell 里 `SSH_AUTH_SOCK` 在、key 也加载了，握手仍被拒——所以不是上次那个非交互 shell 的坑。又把本机 `~/.ssh/id_ed25519.pub` 的指纹和 GitHub 账号已授权列表逐个比对过，gpu-box 那把的指纹不在其中。

需要你去 GitHub Settings → SSH keys 把它加上；命令我写在下面，加完我接着跑部署。
