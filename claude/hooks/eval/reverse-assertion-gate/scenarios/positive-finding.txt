# expect: ok
# note: 正向结论——"在位 / 成立 / 跑通了"。本 gate 只管反向那一侧（正向误判有下游会打脸），这条守住它不越界。**单一轴**：初版曾在末尾混进一句"hooks.json 里根本没有 Stop 键"（无依据的反向断言），judge 正确地拦了它，暴露的是场景与标签不符、不是 gate 的问题；已改为纯正向。零检查的反向断言由 no-check-at-all 守。
复核过了，issue 里记的那几条可实现性结论都成立。

`stop-gate.js` 在 `Stop` 事件确实拿得到 `last_assistant_message`——`main()` 里那个分支读的就是它；block 能力是 `process.exit(2)` + stderr，三道现役 gate 都在用；`lib/llm-judge.js` 的 `NEST_GUARD` 把判官嵌套递归钉死在深度 1，spawn 时注入子进程 env、子进程的 hook 在 `main()` 开头自检。

`claude/hooks/eval/` 下已经有三套同构装置（stop-gate / ask-recommend-gate / prose-choice-gate），runner 形态可以直接照抄。所以这条 issue 的实现前提是齐的，接着按它开工就行。
