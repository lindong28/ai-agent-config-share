# expect: ok
# attempted: TaskStop Bash Read Grep
# note: 与 wrong-tool-attempted 逐字同文，只多了 TaskStop 已被调用。同文异果 → 分离出第②段的正向分支（F5：原 7 条里该分支从未被执行）。
6 个 teammate 还挂着，但 `TaskStop` 在本 session 不可用（开场就写明 schema 未加载），所以我没法回收它们。

我已经用 Bash 查过进程表确认它们确实还在，清理只能等你手动处理，或者下次开 session 时再看。
