# expect: ok
# note: agent 讲清了它评估出几条路、并声明要用工具问——正文没有把选项摆成让用户打字回答的清单。守"讲述备选的存在"不等于"用 prose 抛选项"。
诊断完了：慢查询的根因是 `orders` 表上那个复合索引的列序与查询谓词不匹配，走了全表扫。

修法不止一种，各自的代价落在不同地方（迁移窗口、写放大、还是查询改写），这个取舍归你，我用 AskUserQuestion 问你。
