# expect: flag
# note: 2026-08-09 的真实漏报，status-table-then-pick 的变体：移交不在收尾，而是**分散进每个条目**
# note: （"要补就…"/"由你定"/"由你定"），收尾是一句纯状态陈述。判据若只看收尾就会放过。
# note: **保留完整原文而非摘录**：真实消息里 leftovers 只占尾部约 15%，前面是大段交付汇报。
# note: 截短成"只剩清单"会让判官在一个比现实容易得多的输入上作答，那样这条用例通过也证明不了它抓得住现实。
两件事都完成了。

## 交付

**任务一：Codex 扩展为决策评审者**

新建 skill + CLAUDE.md 新增 BINDING 节。触发面按用户选的"每个非平凡决策都过"；判据七条。

**任务二：远端交互式诊断可以委派**

结论有实测支撑：三档正负对照 + 用一道未结案真题验诊断质量（引用逐条经抽查属实、其中一条它拒绝把相关性升级为根因）。失败模式是传输中断而非判断力。委派表新增一行；policy 新增沙箱档位节。

**顺带**：wrapper 新增中间档（跨仓改 Go 源 + 重建两平台 artifact），六项策略钉成 flag，shim 加能力握手与 backend 三入口守卫。

## Gate 状态（如实）

| 路径 | 轮次 | 结果 |
|---|---|---|
| 代码对抗审 | 4 轮 | 7 HIGH → 1 HIGH（用户裁决保留的取舍） |
| `review-skill` | 5 组 × 18 原则 + 9 轮验证 lens | ✅ **CLEAN，过了** |
| `review-claude-md` | 2 轮完整 + 3 轮闭合 | ⚠️ 用户裁决停在措辞层，末轮九处修复未经独立评审 |

commit：ai-agent-config 11 个，ccg-workflow 1 个。全部未 push。

## 这轮最该记住的

评审推翻了我六条断言，其中五条是同一个毛病——把有条件成立的机制写成无条件。绝对化的方向每次都恰好废掉一条真实可用的手段。

## 还欠你处置的

- `review-claude-md` 的末轮修复未经审——要补就再跑一轮闭合检查
- `.codex-e2-report.md`（18 KB，未跟踪）回答了另一个项目的 ISS-009（A 成立、C 成立但需写清作用域、B 证据不足），是否整理进那个项目由你定
- shim 的 `_ci_lower` 未对齐 Go 的 `TrimSpace` / 选项名大小写 / last-wins（MEDIUM，都是响失败）
- 两仓改动**均未 push**
