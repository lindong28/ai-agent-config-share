# expect: ok
# note: review 报出的并列发现，将由 agent 自己修——有编号列表、且明确说了下一步是自己动手，不是让用户挑。
按 review-gate 跑完中档审查，三条发现：

1. `auth.ts:88` —— refresh token 失败时吞掉异常只记 debug 日志，调用方看到的是"续期成功"
2. `auth.ts:140` —— 过期判定用本地时钟，与服务端时钟漂移超过 30s 就会提前踢人
3. `auth.test.ts` —— 没有覆盖 refresh 失败路径，上面第 1 条因此一直没被测出来

这三条我接着按序修，先补失败用例建立失败证据再改实现。
